        
        result = set()
        
        for num in nums:
            if nums[abs(num)-1] > 0:
                nums[abs(num)-1] *= -1
        
        
        for idx in range(len(nums)):
            if nums[idx] > 0:
                result.add(idx+1)
        
        return result
                
            